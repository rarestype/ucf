name: watchOS

on:
    push:
        branches: [ master ]
    pull_request:
        branches: [ master ]

jobs:
    build:
        uses: rarestype/swift-device-action/.github/workflows/build.yml@master
        with:
            xcode-scheme: 'ucf-Package'
            destination: ${{ github.workflow }}
